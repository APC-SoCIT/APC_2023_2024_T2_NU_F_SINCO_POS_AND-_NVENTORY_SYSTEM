<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Solid color for the shape -->
    <solid android:color="@color/white" />

    <!-- Size of the shape -->
    <size
        android:width="24dp"
        android:height="24dp" />

    <!-- Outline for the shape -->
    <stroke
        android:width="1dp"
        android:color="@color/black"
        />

</shape>
